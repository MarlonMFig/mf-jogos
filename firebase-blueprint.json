{
  "entities": {
    "config/houses_data": {
      "title": "Houses Config",
      "description": "Global list of betting houses maintained by administrator",
      "type": "object",
      "properties": {
        "houses": {
          "type": "array",
          "description": "List of betting houses"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of last update"
        }
      },
      "required": ["houses"]
    },
    "config/admin_settings": {
      "title": "Admin Settings",
      "description": "Global administrator settings including password hash/string",
      "type": "object",
      "properties": {
        "adminPassword": {
          "type": "string",
          "description": "Custom admin panel password"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of last update"
        }
      }
    },
    "stats/analytics": {
      "title": "Site Analytics",
      "description": "Global site statistics tracking visits, platform clicks, and promo code copies",
      "type": "object",
      "properties": {
        "totalVisits": { "type": "number" },
        "totalClicks": { "type": "number" },
        "totalCopies": { "type": "number" },
        "houseClicks": { "type": "object" },
        "houseCopies": { "type": "object" }
      }
    },
    "config/pix_da_sorte": {
      "title": "Pix da Sorte Campaign",
      "description": "Settings and state for the Pix da Sorte RNG giveaway",
      "type": "object",
      "properties": {
        "active": { "type": "boolean" },
        "eventDate": { "type": "string" },
        "pixValue": { "type": "number" },
        "totalPrizes": { "type": "number" },
        "claimedPrizes": { "type": "number" },
        "winOddsPercentage": { "type": "number" },
        "adminInstructions": { "type": "string" },
        "winners": { "type": "array" }
      }
    },
    "pix_ip_attempts/{eventDate}": {
      "title": "Pix da Sorte IP Attempts",
      "description": "IP attempt counters for Pix da Sorte per event date",
      "type": "object",
      "properties": {
        "ipAttempts": { "type": "object" }
      }
    },
    "notification_subscribers/{subId}": {
      "title": "Notification Subscribers",
      "description": "Device tokens and push subscriptions for PWA users",
      "type": "object",
      "properties": {
        "subscribedAt": { "type": "string" },
        "userAgent": { "type": "string" },
        "active": { "type": "boolean" }
      }
    },
    "config/push_notifications": {
      "title": "Push Notification Broadcasts",
      "description": "Sent broadcast notifications log",
      "type": "object",
      "properties": {
        "history": { "type": "array" }
      }
    }
  }
}
